version: '3.9'

services:
 app:
  build:
    context: ./backend
    dockerfile: dockerfile
  container_name: backend
  ports:
   - 8000-8000


 nginix:
  image: nginx:alpine
  container_name: nginx
  ports:
    - "80:80"
  
  